spring:
  cache:
    type: redis
    redis:
      time-to-live: -1 #缓存超时时间ms  -1 表示永久有效
      cache-null-values: false #是否缓存空值
  redis:
    port: 38019
    host: 10.10.20.134
    lettuce:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
    timeout: 10000  #redis 连接超时时间ms
    database: 1
    password: Goldencis@123
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.10.20.134:3306/test?allowPublicKeyRetrieval=true&&useUnicode=true&allowMultiQueries=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: Goldencis1234#
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      minimum-idle: 1
      maximum-pool-size: 100
      auto-commit: true
      idle-timeout: 30000
      pool-name: AdvZuulHikariCP
      max-lifetime: 1800000
      connection-timeout: 30000
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

  mapper-locations: classpath:mappers/*.xml
